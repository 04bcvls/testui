using System;

public class TransmuteSubject
{
	public TransmuteSubject()
	{
		if (Fil >= 60)
		{
			float tFil;
			tFil = (int)(75 + ((Fil - 60) / 1.6));
			Fil == tFil;
		}else if (Fil < 60)
        {
			float tFil;
			tFil = (int)((60 + _aveRes / 4));
			Fil == tFil;
		}
}
